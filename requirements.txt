# Core LangChain package and its dependencies (for chains, LCEL, etc.)
langchain
langchain-core

# Integration for Google LLM (Gemini) and Google Embeddings
langchain-google-genai

# Integration for OpenSearch VectorStore
langchain-community

# Dedicated package for Text Splitters (e.g., RecursiveCharacterTextSplitter)
langchain-text-splitters

# Native Python client for interacting with OpenSearch (used for index deletion/existence checks)
opensearch-py

# Optional: If you use the TikToken encoder for token-based splitting
# tiktoken